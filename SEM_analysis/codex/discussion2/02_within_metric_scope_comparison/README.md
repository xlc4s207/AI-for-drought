# 同一通量内的双口径对比

本目录比较同一通量内部的 `prepeak` 与 `recoverywin` 两套口径。比较重点包括预测表现、主导特征排序、dependence 响应方向以及 SEM 目标路径结构。目录包含 `gpp_prepeak_vs_recoverywin_analysis_cn.md` 与 `reco_prepeak_vs_recoverywin_analysis_cn.md` 两份文稿。
